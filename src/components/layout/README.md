# Layout

Chrome that persists across routes: `Header`, `Footer`, mobile nav, the booking
ticker strip.

Nothing here yet — the scaffold's `layout.tsx` mounts only `SmoothScroll` and
`<main>`. Add `Header.tsx` / `Footer.tsx` here and mount them in
`src/app/layout.tsx` when the real pages start.
